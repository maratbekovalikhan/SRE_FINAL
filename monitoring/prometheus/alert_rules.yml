groups:
  - name: task_api_slo_alerts
    rules:
      # SLO: Availability > 99.9%
      - alert: HighErrorRate
        expr: |
          sum(rate(http_requests_total{status=~"5.."}[5m])) /
          clamp_min(sum(rate(http_requests_total[5m])), 0.001) > 0.001
        for: 2m
        labels:
          severity: critical
          team: sre
        annotations:
          summary: "Error rate exceeds SLO (>0.1%)"
          description: "Current error rate: {{ $value | humanizePercentage }}"

      # SLO: Latency p99 < 500ms
      - alert: HighLatency
        expr: |
          histogram_quantile(0.99,
            sum(rate(http_request_duration_seconds_bucket[5m])) by (le, handler)
          ) > 0.5
        for: 3m
        labels:
          severity: warning
          team: sre
        annotations:
          summary: "p99 latency exceeds 500ms SLO"
          description: "Handler {{ $labels.handler }} p99 latency: {{ $value }}s"

      # Pod down
      - alert: PodCrashLooping
        expr: |
          rate(kube_pod_container_status_restarts_total{namespace="task-api"}[15m]) > 0
        for: 5m
        labels:
          severity: critical
          team: sre
        annotations:
          summary: "Pod {{ $labels.pod }} is crash looping"

      # SLO: Task write success rate > 99%
      - alert: TaskWriteFailureRate
        expr: |
          sum(rate(task_operations_total{operation=~"create|update|delete",result!="success"}[10m])) /
          clamp_min(sum(rate(task_operations_total{operation=~"create|update|delete"}[10m])), 0.001) > 0.01
        for: 10m
        labels:
          severity: warning
          team: sre
        annotations:
          summary: "Task write success ratio fell below 99%"
          description: "Non-successful write operations exceeded the 1% error budget."

  - name: infrastructure_alerts
    rules:
      - alert: NodeHighCPU
        expr: |
          100 - (avg by(instance)(rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Node CPU > 85%"

      - alert: NodeHighMemory
        expr: |
          (1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) * 100 > 85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Node memory > 85%"
